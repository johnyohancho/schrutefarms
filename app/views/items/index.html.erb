<h1>All Items</h1>

<% @items.each do |item| %>
    <img src="<%= item.image_url %>" height="100" width="150">
    <%= link_to item.name, item_path(item) %><br>
    <%= item.description %><br>
    <br>
<% end %>
